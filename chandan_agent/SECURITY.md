# Security

- The AI model MUST NEVER receive unrestricted OS access.
- 4-Level Permission System:
  1. READ
  2. EXECUTE
  3. EXTERNAL ACTION
  4. DANGEROUS ACTION
- All external API keys are managed securely and not exposed to the AI model directly.
- Secure device pairing required for remote access.
